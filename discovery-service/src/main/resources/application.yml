spring:
  application:
    name: discovery-service
server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  instance:
    prefer-ip-address: true
management:
  endpoints:
    jmx:
      exposure:
        include: health
